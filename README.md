# Taller-11
Alumno: Carlos torres del rio. Ejercicio 11 de POO

Objetivo: Declarar las diferencia de una clase abstacta y una normal.

Primer arhcivo: Figura.java
-el taller pide una clase abstracata figura (Hecha) y 2 sub clases
-Figura(Hecha): Contiene un metodo abstracto sin datos, se pide en el taller que se sobreescriba.
    -Circulo (Hecho); Esta funciona como una sobreescritura de el metodo presente en Figura
    -Recangulo (Heco): Funciona igual que la anterior, solo se variaron los mensajes
-Es incomodo usar un main externo. Modifica el codigo mas tarde para hacerlo mas simple.

Se sobreescribio bien el metodo en figura. Funciona en ambas clases con sus mensajes diferentes, edita el main.

Se hicieron 2 objetos con el mismo nombre que la clase para la que corresponden pero en minuscla. Se hizo que llamaran al metodo de su respectiva clase.

Se consiguio que el codigo fuera funcionalmas haya del estructurado, organiza el main.
El main ya no es un archivo separado, actualizalo en github.
El main es una clase interna del archivo para simplificarlo mas y no recurir a un tostring innecesario.

El codigo parece estar completo.
Actulizar el readme despues de hacer el video.

